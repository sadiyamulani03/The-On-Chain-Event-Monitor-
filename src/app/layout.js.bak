import './globals.css'
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Chain Event Monitor',
  description: 'Raw data decoder turning calldata and event logs into human-readable transaction stories via RPC subscriptions/websockets',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
